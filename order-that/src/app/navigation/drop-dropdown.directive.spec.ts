import { DropDropdownDirective } from './drop-dropdown.directive';

describe('DropDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new DropDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
